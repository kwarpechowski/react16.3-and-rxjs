import { createContext } from 'react';
export const CounterContext =  createContext({value: null});
export const NamesContext =  createContext({value: null});
export const MergeContext =   createContext();
